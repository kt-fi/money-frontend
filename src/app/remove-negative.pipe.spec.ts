import { RemoveNegativePipe } from './remove-negative.pipe';

describe('RemoveNegativePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNegativePipe();
    expect(pipe).toBeTruthy();
  });
});
